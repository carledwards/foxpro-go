# Compiled binaries
bin/
*.exe
*.out

# Test / coverage / profile artifacts
*.test
*.prof
coverage.txt
coverage.out

# wasm-hello build artifacts — rebuilt by `make build` locally and by
# the GitHub Pages workflow on every push. Source-of-truth lives in
# main.go, sim.js, index.html (and wasm/foxpro.js, copied in by make).
examples/wasm-hello/web/sim.wasm
examples/wasm-hello/web/wasm_exec.js
examples/wasm-hello/web/foxpro.js

# macOS / editor noise
.DS_Store
.idea/
.vscode/
*.swp
